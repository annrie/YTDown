# スケジュールダウンロード 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** cron式で指定した日時に自動ダウンロード・チャンネル監視を実現するスケジュール機能を追加する

**Architecture:** Rustバックエンドに `tokio-cron-scheduler` を追加し、`AppState` でスケジューラを管理する。スケジュール情報はSQLiteに保存し、アプリ起動時・スリープ復帰時にスキップ判定を行う。フロントエンドはPiniaストア＋Vue 3コンポーネントで管理画面を構築し、既存の `DownloadDialog` にスケジュール登録オプションを追加する。

**Tech Stack:** Rust / tokio-cron-scheduler 0.13 / cron 0.12 / rusqlite / Vue 3 Composition API / Pinia / croner (npm) / tauri-plugin-notification 2

**仕様書:** `docs/superpowers/specs/2026-03-28-schedule-download-design.md`

---

## ファイル構成

### 新規作成
| ファイル | 役割 |
|---|---|
| `src-tauri/src/commands/schedules.rs` | Tauri IPCコマンド 7つ |
| `src-tauri/src/scheduler.rs` | スケジューラ初期化・ジョブ管理 |
| `src-tauri/permissions/autogenerated/create_schedule.toml` | 自動生成パーミッション |
| `src-tauri/permissions/autogenerated/update_schedule.toml` | 同上 |
| `src-tauri/permissions/autogenerated/delete_schedule.toml` | 同上 |
| `src-tauri/permissions/autogenerated/toggle_schedule.toml` | 同上 |
| `src-tauri/permissions/autogenerated/list_schedules.toml` | 同上 |
| `src-tauri/permissions/autogenerated/get_schedule.toml` | 同上 |
| `src-tauri/permissions/autogenerated/run_schedule_now.toml` | 同上 |
| `src/stores/schedules.ts` | Piniaストア |
| `src/components/schedules/ScheduleView.vue` | 管理画面 |
| `src/components/schedules/ScheduleDialog.vue` | 作成/編集ダイアログ |
| `src/components/schedules/ScheduleCard.vue` | 一覧カード |

### 既存ファイルの変更
| ファイル | 変更内容 |
|---|---|
| `src-tauri/src/db/schema.sql` | `schedules` テーブル追加 |
| `src-tauri/src/db/models.rs` | `Schedule` 構造体追加 |
| `src-tauri/src/db/queries.rs` | スケジュールCRUDクエリ追加 |
| `src-tauri/src/state.rs` | `scheduler` フィールド追加 |
| `src-tauri/src/commands/mod.rs` | `schedules` モジュール宣言 |
| `src-tauri/src/lib.rs` | コマンド登録・スケジューラ初期化・通知プラグイン |
| `src-tauri/Cargo.toml` | `tokio-cron-scheduler`・`tauri-plugin-notification` 追加 |
| `src-tauri/capabilities/default.json` | 7コマンド＋通知パーミッション追加 |
| `src/types/index.ts` | `Schedule` インターフェース・`SidebarSection` 更新 |
| `src/App.vue` | スケジュール画面のルーティング追加 |
| `src/components/layout/AppSidebar.vue` | スケジュールセクション追加 |
| `src/components/download/DownloadDialog.vue` | スケジュール登録トグル追加 |

---

## Task 1: DBスキーマと Rust モデル

**Files:**
- Modify: `src-tauri/src/db/schema.sql`
- Modify: `src-tauri/src/db/models.rs`

- [ ] **Step 1: schema.sql に schedules テーブルを追記**

`src-tauri/src/db/schema.sql` の末尾に追加:

```sql
CREATE TABLE IF NOT EXISTS schedules (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  name         TEXT NOT NULL,
  url          TEXT NOT NULL,
  cron_expr    TEXT NOT NULL,
  options_json TEXT NOT NULL,
  is_active    INTEGER NOT NULL DEFAULT 1,
  is_channel   INTEGER NOT NULL DEFAULT 0,
  last_error   TEXT,
  fail_count   INTEGER NOT NULL DEFAULT 0,
  is_running   INTEGER NOT NULL DEFAULT 0,
  last_run_at  TEXT,
  next_run_at  TEXT,
  created_at   TEXT NOT NULL DEFAULT (datetime('now'))
);
```

- [ ] **Step 2: models.rs に Schedule 構造体を追加**

`src-tauri/src/db/models.rs` に追加:

```rust
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Schedule {
    pub id: i64,
    pub name: String,
    pub url: String,
    pub cron_expr: String,
    pub options_json: String,
    pub is_active: bool,
    pub is_channel: bool,
    pub last_error: Option<String>,
    pub fail_count: i64,
    pub is_running: bool,
    pub last_run_at: Option<String>,
    pub next_run_at: Option<String>,
    pub created_at: String,
}
```

- [ ] **Step 3: cargo check で確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5
```
期待: `error[E...]` が出ないこと

- [ ] **Step 4: コミット**

```bash
git add src-tauri/src/db/schema.sql src-tauri/src/db/models.rs
git commit --no-gpg-sign -m "feat(schedules): add schedules table schema and Schedule model"
```

---

## Task 2: DBクエリ

**Files:**
- Modify: `src-tauri/src/db/queries.rs`

- [ ] **Step 1: queries.rs にスケジュールCRUDを追加**

`src-tauri/src/db/queries.rs` の末尾に追加:

```rust
// === Schedules ===

pub fn insert_schedule(
    conn: &Connection,
    name: &str,
    url: &str,
    cron_expr: &str,
    options_json: &str,
    is_channel: bool,
) -> SqlResult<i64> {
    conn.execute(
        "INSERT INTO schedules (name, url, cron_expr, options_json, is_channel)
         VALUES (?1, ?2, ?3, ?4, ?5)",
        params![name, url, cron_expr, options_json, is_channel as i64],
    )?;
    Ok(conn.last_insert_rowid())
}

pub fn get_schedule(conn: &Connection, id: i64) -> SqlResult<Schedule> {
    conn.query_row(
        "SELECT id, name, url, cron_expr, options_json, is_active, is_channel,
                last_error, fail_count, is_running, last_run_at, next_run_at, created_at
         FROM schedules WHERE id = ?1",
        params![id],
        |row| Ok(Schedule {
            id: row.get(0)?,
            name: row.get(1)?,
            url: row.get(2)?,
            cron_expr: row.get(3)?,
            options_json: row.get(4)?,
            is_active: row.get::<_, i64>(5)? != 0,
            is_channel: row.get::<_, i64>(6)? != 0,
            last_error: row.get(7)?,
            fail_count: row.get(8)?,
            is_running: row.get::<_, i64>(9)? != 0,
            last_run_at: row.get(10)?,
            next_run_at: row.get(11)?,
            created_at: row.get(12)?,
        }),
    )
}

pub fn list_schedules(conn: &Connection) -> SqlResult<Vec<Schedule>> {
    let mut stmt = conn.prepare(
        "SELECT id, name, url, cron_expr, options_json, is_active, is_channel,
                last_error, fail_count, is_running, last_run_at, next_run_at, created_at
         FROM schedules ORDER BY created_at DESC",
    )?;
    let rows = stmt.query_map([], |row| {
        Ok(Schedule {
            id: row.get(0)?,
            name: row.get(1)?,
            url: row.get(2)?,
            cron_expr: row.get(3)?,
            options_json: row.get(4)?,
            is_active: row.get::<_, i64>(5)? != 0,
            is_channel: row.get::<_, i64>(6)? != 0,
            last_error: row.get(7)?,
            fail_count: row.get(8)?,
            is_running: row.get::<_, i64>(9)? != 0,
            last_run_at: row.get(10)?,
            next_run_at: row.get(11)?,
            created_at: row.get(12)?,
        })
    })?;
    rows.collect()
}

pub fn list_active_schedules(conn: &Connection) -> SqlResult<Vec<Schedule>> {
    let mut stmt = conn.prepare(
        "SELECT id, name, url, cron_expr, options_json, is_active, is_channel,
                last_error, fail_count, is_running, last_run_at, next_run_at, created_at
         FROM schedules WHERE is_active = 1 ORDER BY created_at DESC",
    )?;
    let rows = stmt.query_map([], |row| {
        Ok(Schedule {
            id: row.get(0)?,
            name: row.get(1)?,
            url: row.get(2)?,
            cron_expr: row.get(3)?,
            options_json: row.get(4)?,
            is_active: row.get::<_, i64>(5)? != 0,
            is_channel: row.get::<_, i64>(6)? != 0,
            last_error: row.get(7)?,
            fail_count: row.get(8)?,
            is_running: row.get::<_, i64>(9)? != 0,
            last_run_at: row.get(10)?,
            next_run_at: row.get(11)?,
            created_at: row.get(12)?,
        })
    })?;
    rows.collect()
}

pub fn update_schedule(
    conn: &Connection,
    id: i64,
    name: &str,
    url: &str,
    cron_expr: &str,
    options_json: &str,
    is_channel: bool,
) -> SqlResult<()> {
    conn.execute(
        "UPDATE schedules SET name=?1, url=?2, cron_expr=?3, options_json=?4, is_channel=?5
         WHERE id=?6",
        params![name, url, cron_expr, options_json, is_channel as i64, id],
    )?;
    Ok(())
}

pub fn delete_schedule(conn: &Connection, id: i64) -> SqlResult<()> {
    conn.execute("DELETE FROM schedules WHERE id = ?1", params![id])?;
    Ok(())
}

pub fn toggle_schedule(conn: &Connection, id: i64, is_active: bool) -> SqlResult<()> {
    conn.execute(
        "UPDATE schedules SET is_active = ?1 WHERE id = ?2",
        params![is_active as i64, id],
    )?;
    Ok(())
}

pub fn set_schedule_running(conn: &Connection, id: i64, is_running: bool) -> SqlResult<()> {
    conn.execute(
        "UPDATE schedules SET is_running = ?1 WHERE id = ?2",
        params![is_running as i64, id],
    )?;
    Ok(())
}

pub fn record_schedule_success(
    conn: &Connection,
    id: i64,
    last_run_at: &str,
    next_run_at: Option<&str>,
) -> SqlResult<()> {
    conn.execute(
        "UPDATE schedules SET fail_count=0, last_error=NULL, is_running=0,
                              last_run_at=?1, next_run_at=?2 WHERE id=?3",
        params![last_run_at, next_run_at, id],
    )?;
    Ok(())
}

pub fn record_schedule_failure(
    conn: &Connection,
    id: i64,
    error: &str,
    next_run_at: Option<&str>,
) -> SqlResult<()> {
    conn.execute(
        "UPDATE schedules SET fail_count = fail_count + 1, last_error=?1,
                              is_running=0, next_run_at=?2 WHERE id=?3",
        params![error, next_run_at, id],
    )?;
    Ok(())
}

pub fn disable_schedule(conn: &Connection, id: i64) -> SqlResult<()> {
    conn.execute(
        "UPDATE schedules SET is_active=0 WHERE id=?1",
        params![id],
    )?;
    Ok(())
}

pub fn list_overdue_schedules(conn: &Connection, now: &str) -> SqlResult<Vec<Schedule>> {
    let mut stmt = conn.prepare(
        "SELECT id, name, url, cron_expr, options_json, is_active, is_channel,
                last_error, fail_count, is_running, last_run_at, next_run_at, created_at
         FROM schedules WHERE is_active=1 AND next_run_at IS NOT NULL AND next_run_at < ?1",
    )?;
    let rows = stmt.query_map(params![now], |row| {
        Ok(Schedule {
            id: row.get(0)?,
            name: row.get(1)?,
            url: row.get(2)?,
            cron_expr: row.get(3)?,
            options_json: row.get(4)?,
            is_active: row.get::<_, i64>(5)? != 0,
            is_channel: row.get::<_, i64>(6)? != 0,
            last_error: row.get(7)?,
            fail_count: row.get(8)?,
            is_running: row.get::<_, i64>(9)? != 0,
            last_run_at: row.get(10)?,
            next_run_at: row.get(11)?,
            created_at: row.get(12)?,
        })
    })?;
    rows.collect()
}

pub fn update_schedule_next_run(conn: &Connection, id: i64, next_run_at: Option<&str>) -> SqlResult<()> {
    conn.execute(
        "UPDATE schedules SET next_run_at=?1 WHERE id=?2",
        params![next_run_at, id],
    )?;
    Ok(())
}
```

- [ ] **Step 2: cargo check で確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5
```
期待: `error[E...]` が出ないこと

- [ ] **Step 3: コミット**

```bash
git add src-tauri/src/db/queries.rs
git commit --no-gpg-sign -m "feat(schedules): add schedule CRUD queries"
```

---

## Task 3: 依存関係の追加

**Files:**
- Modify: `src-tauri/Cargo.toml`

- [ ] **Step 1: Cargo.toml に新依存を追加**

`[dependencies]` セクションに追記:

```toml
tokio-cron-scheduler = "0.13"
tauri-plugin-notification = "2"
cron = "0.12"   # compute_next_run で cron 式から次回発火時刻を計算するために使用
```

- [ ] **Step 2: cargo check で依存解決確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -10
```
期待: `tokio-cron-scheduler` と `tauri-plugin-notification` がダウンロード・コンパイルされてエラーなし

- [ ] **Step 3: コミット**

```bash
git add src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit --no-gpg-sign -m "chore(deps): add tokio-cron-scheduler and tauri-plugin-notification"
```

---

## Task 4: AppState 更新とスケジューラモジュール

**Files:**
- Modify: `src-tauri/src/state.rs`
- Create: `src-tauri/src/scheduler.rs`

- [ ] **Step 1: AppState に scheduler フィールドを追加**

`src-tauri/src/state.rs` を以下に更新:

```rust
use std::collections::HashMap;
use std::sync::Arc;
use tokio::sync::Mutex;
use tokio_cron_scheduler::JobScheduler;

pub struct ActiveDownload {
    #[allow(dead_code)]
    pub download_id: i64,
    pub pid: u32,
    pub paused: bool,
}

pub struct AppState {
    pub db: Arc<Mutex<rusqlite::Connection>>,
    pub active_downloads: Arc<Mutex<HashMap<i64, ActiveDownload>>>,
    pub ytdlp_path: Arc<Mutex<Option<String>>>,
    pub scheduler: Arc<Mutex<JobScheduler>>,
}

impl AppState {
    pub fn new(db: rusqlite::Connection, scheduler: JobScheduler) -> Self {
        Self {
            db: Arc::new(Mutex::new(db)),
            active_downloads: Arc::new(Mutex::new(HashMap::new())),
            ytdlp_path: Arc::new(Mutex::new(None)),
            scheduler: Arc::new(Mutex::new(scheduler)),
        }
    }
}
```

- [ ] **Step 2: scheduler.rs を作成**

`src-tauri/src/scheduler.rs` を新規作成:

```rust
use std::sync::Arc;
use tauri::{AppHandle, Manager};
use tokio::sync::Mutex;
use tokio_cron_scheduler::{Job, JobScheduler};
use chrono::Utc;

use crate::state::AppState;
use crate::db::queries;

/// アプリ起動時・スリープ復帰時のスキップ判定と通知
pub async fn check_overdue_schedules(app: &AppHandle) {
    let state = app.state::<AppState>();
    let now = Utc::now().to_rfc3339();
    let overdue = {
        let db = state.db.lock().await;
        queries::list_overdue_schedules(&db, &now).unwrap_or_default()
    };

    for schedule in overdue {
        // next_run_at を次回将来時刻に進める (スキップ)
        let next = compute_next_run(&schedule.cron_expr);
        {
            let db = state.db.lock().await;
            let _ = queries::update_schedule_next_run(&db, schedule.id, next.as_deref());
        }
        // macOS 通知
        send_notification(
            app,
            "スケジュールをスキップしました",
            &format!("「{}」のスケジュールが実行されませんでした（アプリが起動していませんでした）", schedule.name),
        );
    }
}

/// 全アクティブスケジュールを JobScheduler に登録する
pub async fn register_all_jobs(app: &AppHandle) {
    let state = app.state::<AppState>();
    let schedules = {
        let db = state.db.lock().await;
        queries::list_active_schedules(&db).unwrap_or_default()
    };

    for schedule in schedules {
        let _ = register_job(app, schedule.id, &schedule.cron_expr).await;
    }
}

/// 1つのスケジュールを JobScheduler に登録する
/// 既存ジョブがあればキャンセルしてから再登録する
pub async fn register_job(app: &AppHandle, schedule_id: i64, cron_expr: &str) -> Result<(), String> {
    let state = app.state::<AppState>();
    let app_clone = app.clone();
    let cron = cron_expr.to_string();

    let job = Job::new_async(cron.as_str(), move |_uuid, _lock| {
        let app = app_clone.clone();
        Box::pin(async move {
            execute_schedule(&app, schedule_id).await;
        })
    })
    .map_err(|e| e.to_string())?;

    // next_run_at を更新
    let next = compute_next_run(cron_expr);
    {
        let db = state.db.lock().await;
        let _ = queries::update_schedule_next_run(&db, schedule_id, next.as_deref());
    }

    let mut sched = state.scheduler.lock().await;
    sched.add(job).await.map_err(|e| e.to_string())?;
    Ok(())
}

/// スケジュール実行本体
pub async fn execute_schedule(app: &AppHandle, schedule_id: i64) {
    let state = app.state::<AppState>();

    // 重複実行防止
    let schedule = {
        let db = state.db.lock().await;
        match queries::get_schedule(&db, schedule_id) {
            Ok(s) => s,
            Err(_) => return,
        }
    };
    if schedule.is_running || !schedule.is_active {
        return;
    }

    // is_running = true
    {
        let db = state.db.lock().await;
        let _ = queries::set_schedule_running(&db, schedule_id, true);
    }

    let result = run_download(app, &schedule).await;
    let now = Utc::now().to_rfc3339();
    let next = compute_next_run(&schedule.cron_expr);

    {
        let db = state.db.lock().await;
        match result {
            Ok(_) => {
                let _ = queries::record_schedule_success(&db, schedule_id, &now, next.as_deref());
            }
            Err(ref e) => {
                let _ = queries::record_schedule_failure(&db, schedule_id, e, next.as_deref());
                // 3回連続失敗で自動無効化
                let s = queries::get_schedule(&db, schedule_id).ok();
                if let Some(s) = s {
                    if s.fail_count >= 3 {
                        let _ = queries::disable_schedule(&db, schedule_id);
                        send_notification(
                            app,
                            "スケジュール自動無効化",
                            &format!("「{}」が3回連続で失敗したため無効化されました", schedule.name),
                        );
                    }
                }
            }
        }
    }

    // フロントエンドに通知
    let _ = app.emit("schedule-fired", schedule_id);
}

/// yt-dlp ダウンロード実行 (start_download の内部ロジックを呼び出す)
async fn run_download(app: &AppHandle, schedule: &crate::db::models::Schedule) -> Result<(), String> {
    use crate::commands::download::DownloadOptions;

    let options: DownloadOptions = serde_json::from_str(&schedule.options_json)
        .map_err(|e| format!("オプションのパースに失敗: {}", e))?;

    // チャンネルモード: --dateafter を追加
    let url = if schedule.is_channel {
        schedule.url.clone()
    } else {
        schedule.url.clone()
    };

    let state = app.state::<AppState>();
    crate::commands::download::run_download_internal(app, url, options, schedule.is_channel, schedule.last_run_at.clone(), &state).await
}

/// cron式から次回発火時刻を計算 (ISO8601)
/// `cron = "0.12"` クレートを使用する
pub fn compute_next_run(cron_expr: &str) -> Option<String> {
    use cron::Schedule;
    use std::str::FromStr;
    use chrono::Utc;

    let schedule = Schedule::from_str(cron_expr).ok()?;
    let next = schedule.upcoming(Utc).next()?;
    Some(next.to_rfc3339())
}

/// ジョブを JobScheduler から除去する（toggle OFF 時に使用）
/// UUID はスケジュールIDをキーとする HashMap で管理する
/// （実装時: AppState に `schedule_job_ids: Arc<Mutex<HashMap<i64, Uuid>>>` を追加して対応）
pub async fn cancel_job(app: &AppHandle, schedule_id: i64) {
    let state = app.state::<AppState>();
    // job_ids マップから UUID を取得してジョブを除去する
    // 実装時は AppState に job_ids フィールドを追加して UUID を保持すること
    // 例:
    // if let Some(uuid) = state.schedule_job_ids.lock().await.remove(&schedule_id) {
    //     let mut sched = state.scheduler.lock().await;
    //     let _ = sched.remove(&uuid).await;
    // }
    let _ = schedule_id; // 上記実装まで仮
}

/// macOS 通知を送信
fn send_notification(app: &AppHandle, title: &str, body: &str) {
    use tauri_plugin_notification::NotificationExt;
    let _ = app.notification()
        .builder()
        .title(title)
        .body(body)
        .show();
}
```

> **実装メモ**: `compute_next_run` は `tokio-cron-scheduler 0.13` の API に合わせて実装する。`cron` クレート (`cron = "0.12"`) を追加するか、スケジューラの `next_tick_for_job` を使う方法を検討すること。

- [ ] **Step 3: cargo check で確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -10
```

- [ ] **Step 4: コミット**

```bash
git add src-tauri/src/state.rs src-tauri/src/scheduler.rs
git commit --no-gpg-sign -m "feat(schedules): add scheduler module and update AppState"
```

---

## Task 5: スケジュールコマンド

**Files:**
- Create: `src-tauri/src/commands/schedules.rs`
- Modify: `src-tauri/src/commands/mod.rs`
- Modify: `src-tauri/src/commands/download.rs` (`run_download_internal` を pub に切り出す)

- [ ] **Step 1: download.rs から run_download_internal を切り出す**

`src-tauri/src/commands/download.rs` の `start_download` 内部のyt-dlp起動ロジックを `pub async fn run_download_internal(...)` として切り出す。既存の `start_download` はこれを呼び出す薄いラッパーにする。詳細は既存コードを読んで判断すること。

- [ ] **Step 2: schedules.rs を作成**

```rust
use tauri::{AppHandle, State};
use crate::state::AppState;
use crate::db::{queries, models::Schedule};
use crate::scheduler;

#[tauri::command]
pub async fn create_schedule(
    app: AppHandle,
    name: String,
    url: String,
    cron_expr: String,
    options_json: String,
    is_channel: bool,
    state: State<'_, AppState>,
) -> Result<i64, String> {
    let id = {
        let db = state.db.lock().await;
        queries::insert_schedule(&db, &name, &url, &cron_expr, &options_json, is_channel)
            .map_err(|e| e.to_string())?
    };
    scheduler::register_job(&app, id, &cron_expr).await?;
    Ok(id)
}

#[tauri::command]
pub async fn update_schedule(
    app: AppHandle,
    id: i64,
    name: String,
    url: String,
    cron_expr: String,
    options_json: String,
    is_channel: bool,
    state: State<'_, AppState>,
) -> Result<(), String> {
    {
        let db = state.db.lock().await;
        queries::update_schedule(&db, id, &name, &url, &cron_expr, &options_json, is_channel)
            .map_err(|e| e.to_string())?;
    }
    // ジョブ再登録 (既存ジョブは scheduler 内でキャンセルして再追加)
    scheduler::register_job(&app, id, &cron_expr).await?;
    Ok(())
}

#[tauri::command]
pub async fn delete_schedule(
    id: i64,
    state: State<'_, AppState>,
) -> Result<(), String> {
    // スコープ付きロック（プロジェクト規約）
    let result = {
        let db = state.db.lock().await;
        queries::delete_schedule(&db, id).map_err(|e| e.to_string())
    };
    result
}

#[tauri::command]
pub async fn toggle_schedule(
    app: AppHandle,
    id: i64,
    is_active: bool,
    state: State<'_, AppState>,
) -> Result<(), String> {
    let cron_expr = {
        let db = state.db.lock().await;
        queries::toggle_schedule(&db, id, is_active).map_err(|e| e.to_string())?;
        queries::get_schedule(&db, id).map(|s| s.cron_expr).map_err(|e| e.to_string())?
    };
    if is_active {
        scheduler::register_job(&app, id, &cron_expr).await?;
    } else {
        // OFFの場合: JobScheduler からジョブを除去する
        scheduler::cancel_job(&app, id).await;
    }
    Ok(())
}

#[tauri::command]
pub async fn list_schedules(
    state: State<'_, AppState>,
) -> Result<Vec<Schedule>, String> {
    // スコープ付きロック（プロジェクト規約）
    let result = {
        let db = state.db.lock().await;
        queries::list_schedules(&db).map_err(|e| e.to_string())
    };
    result
}

#[tauri::command]
pub async fn get_schedule(
    id: i64,
    state: State<'_, AppState>,
) -> Result<Schedule, String> {
    // スコープ付きロック（プロジェクト規約）
    let result = {
        let db = state.db.lock().await;
        queries::get_schedule(&db, id).map_err(|e| e.to_string())
    };
    result
}

#[tauri::command]
pub async fn run_schedule_now(
    id: i64,
    app: AppHandle,
    _state: State<'_, AppState>,
) -> Result<(), String> {
    scheduler::execute_schedule(&app, id).await;
    Ok(())
}
```

- [ ] **Step 3: commands/mod.rs にモジュール宣言を追加**

`src-tauri/src/commands/mod.rs` に追加:

```rust
pub mod schedules;
```

- [ ] **Step 4: cargo check で確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -10
```

- [ ] **Step 5: コミット**

```bash
git add src-tauri/src/commands/schedules.rs src-tauri/src/commands/mod.rs src-tauri/src/commands/download.rs
git commit --no-gpg-sign -m "feat(schedules): add schedule Tauri commands"
```

---

## Task 6: lib.rs の配線

**Files:**
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: lib.rs を更新**

```rust
mod commands;
mod db;
mod scheduler;
mod state;
mod ytdlp;
mod images;  // 既存モジュール — 削除しないこと

use state::AppState;
use tauri::Manager;
use tokio_cron_scheduler::JobScheduler;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .plugin(tauri_plugin_dialog::init())
        .plugin(tauri_plugin_notification::init())   // 追加
        .setup(|app| {
            let app_data_dir = app.path().app_data_dir()
                .expect("Failed to get app data dir");
            let conn = db::init_db(&app_data_dir)
                .expect("Failed to initialize database");

            // スケジューラを非同期初期化
            let scheduler = tauri::async_runtime::block_on(async {
                JobScheduler::new().await.expect("Failed to create scheduler")
            });

            app.manage(AppState::new(conn, scheduler));

            // スケジューラ起動・ジョブ登録・スキップ判定
            let app_handle = app.handle().clone();
            tauri::async_runtime::spawn(async move {
                // スケジューラ開始
                {
                    let state = app_handle.state::<AppState>();
                    let mut sched = state.scheduler.lock().await;
                    sched.start().await.expect("Failed to start scheduler");
                }
                // 全ジョブ登録
                scheduler::register_all_jobs(&app_handle).await;
                // 起動時スキップ判定
                scheduler::check_overdue_schedules(&app_handle).await;

                // スリープ復帰時のスキップ判定: window:focus イベントをリッスン
                let app_focus = app_handle.clone();
                app_handle.listen("tauri://focus", move |_| {
                    let app = app_focus.clone();
                    tauri::async_runtime::spawn(async move {
                        scheduler::check_overdue_schedules(&app).await;
                    });
                });
            });

            Ok(())
        })
        .invoke_handler(tauri::generate_handler![
            // ... 既存のコマンド一覧 ...
            // Schedules (追加)
            commands::schedules::create_schedule,
            commands::schedules::update_schedule,
            commands::schedules::delete_schedule,
            commands::schedules::toggle_schedule,
            commands::schedules::list_schedules,
            commands::schedules::get_schedule,
            commands::schedules::run_schedule_now,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

> 既存の `invoke_handler` リストは全件保持したまま、スケジュール7コマンドを末尾に追加すること。

- [ ] **Step 2: cargo check で確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -10
```

- [ ] **Step 3: コミット**

```bash
git add src-tauri/src/lib.rs
git commit --no-gpg-sign -m "feat(schedules): wire scheduler and commands in lib.rs"
```

---

## Task 7: パーミッションファイル

**Files:**
- Modify: `src-tauri/capabilities/default.json`
- Create: 各 `src-tauri/permissions/autogenerated/*.toml`

- [ ] **Step 1: capabilities/default.json に権限を追加**

`"permissions"` 配列に追加:

```json
"notification:default",
"notification:allow-send-notification",
"allow-create-schedule",
"allow-update-schedule",
"allow-delete-schedule",
"allow-toggle-schedule",
"allow-list-schedules",
"allow-get-schedule",
"allow-run-schedule-now"
```

- [ ] **Step 2: autogenerated TOML ファイルを作成**

以下の7ファイルを作成（既存の `save_url_history.toml` を参考にフォーマットを合わせること）:

- `src-tauri/permissions/autogenerated/create_schedule.toml`
- `src-tauri/permissions/autogenerated/update_schedule.toml`
- `src-tauri/permissions/autogenerated/delete_schedule.toml`
- `src-tauri/permissions/autogenerated/toggle_schedule.toml`
- `src-tauri/permissions/autogenerated/list_schedules.toml`
- `src-tauri/permissions/autogenerated/get_schedule.toml`
- `src-tauri/permissions/autogenerated/run_schedule_now.toml`

各ファイルの内容（例: `create_schedule.toml`）:

```toml
# Automatically generated - DO NOT EDIT!

[[permission]]
identifier = "allow-create-schedule"
description = "Enables the create_schedule command without any pre-configured scope."
commands.allow = ["create_schedule"]
```

- [ ] **Step 3: cargo check で確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -10
```

- [ ] **Step 4: コミット**

```bash
git add src-tauri/capabilities/default.json src-tauri/permissions/autogenerated/
git commit --no-gpg-sign -m "feat(schedules): add Tauri permissions for schedule commands"
```

---

## Task 8: フロントエンド型定義

**Files:**
- Modify: `src/types/index.ts`

- [ ] **Step 1: Schedule インターフェースと SidebarSection を更新**

`src/types/index.ts` に追加・更新:

```typescript
// SidebarSection に 'schedules' を追加
export type SidebarSection =
  | 'downloads-active'
  | 'downloads-completed'
  | 'library-all'
  | 'library-video'
  | 'library-audio'
  | 'images-download'
  | 'images-gallery'
  | 'schedules'      // 追加
  | 'settings'

// Schedule インターフェースを追加
export interface Schedule {
  id: number
  name: string
  url: string
  cron_expr: string
  options_json: string
  is_active: boolean
  is_channel: boolean
  last_error: string | null
  fail_count: number
  is_running: boolean
  last_run_at: string | null
  next_run_at: string | null
  created_at: string
}
```

- [ ] **Step 2: 型チェック**

```bash
npx vue-tsc --noEmit 2>&1 | tail -10
```

- [ ] **Step 3: コミット**

```bash
git add src/types/index.ts
git commit --no-gpg-sign -m "feat(schedules): add Schedule type and update SidebarSection"
```

---

## Task 9: Pinia ストア

**Files:**
- Create: `src/stores/schedules.ts`

- [ ] **Step 1: npm パッケージを追加**

```bash
pnpm add croner
```

- [ ] **Step 2: schedules.ts を作成**

```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { invoke } from '@tauri-apps/api/core'
import { listen } from '@tauri-apps/api/event'
import type { Schedule } from '../types'

export const useSchedulesStore = defineStore('schedules', () => {
  const schedules = ref<Schedule[]>([])

  async function fetchSchedules() {
    schedules.value = await invoke<Schedule[]>('list_schedules')
  }

  async function createSchedule(payload: {
    name: string
    url: string
    cron_expr: string
    options_json: string
    is_channel: boolean
  }): Promise<number> {
    const id = await invoke<number>('create_schedule', payload)
    await fetchSchedules()
    return id
  }

  async function updateSchedule(payload: {
    id: number
    name: string
    url: string
    cron_expr: string
    options_json: string
    is_channel: boolean
  }) {
    await invoke('update_schedule', payload)
    await fetchSchedules()
  }

  async function deleteSchedule(id: number) {
    await invoke('delete_schedule', { id })
    await fetchSchedules()
  }

  async function toggleSchedule(id: number, is_active: boolean) {
    await invoke('toggle_schedule', { id, is_active })
    await fetchSchedules()
  }

  async function runNow(id: number) {
    await invoke('run_schedule_now', { id })
  }

  async function setupScheduleListener() {
    await listen('schedule-fired', () => {
      fetchSchedules()
    })
  }

  return {
    schedules,
    fetchSchedules,
    createSchedule,
    updateSchedule,
    deleteSchedule,
    toggleSchedule,
    runNow,
    setupScheduleListener,
  }
})
```

- [ ] **Step 3: 型チェック**

```bash
npx vue-tsc --noEmit 2>&1 | tail -10
```

- [ ] **Step 4: コミット**

```bash
git add src/stores/schedules.ts
git commit --no-gpg-sign -m "feat(schedules): add schedules Pinia store"
```

---

## Task 10: ScheduleCard コンポーネント

**Files:**
- Create: `src/components/schedules/ScheduleCard.vue`

- [ ] **Step 1: ScheduleCard.vue を作成**

```vue
<script setup lang="ts">
import type { Schedule } from '../../types'

const props = defineProps<{ schedule: Schedule }>()
const emit = defineEmits<{
  toggle: [id: number, is_active: boolean]
  edit: [schedule: Schedule]
  delete: [id: number]
  runNow: [id: number]
}>()

function formatNextRun(iso: string | null): string {
  if (!iso) return '未設定'
  return new Date(iso).toLocaleString('ja-JP')
}
</script>

<template>
  <div class="schedule-card" :class="{ inactive: !schedule.is_active }">
    <div class="card-header">
      <span class="card-name">{{ schedule.name }}</span>
      <label class="toggle-switch">
        <input type="checkbox" :checked="schedule.is_active"
               @change="emit('toggle', schedule.id, !schedule.is_active)" />
        <span class="slider" />
      </label>
    </div>

    <div class="card-meta">
      <span class="meta-item">
        <svg class="meta-icon" viewBox="0 0 16 16" fill="currentColor">
          <path d="M8 3.5a.5.5 0 00-1 0V9a.5.5 0 00.252.434l3.5 2a.5.5 0 00.496-.868L8 8.71V3.5z"/>
          <path d="M8 16A8 8 0 108 0a8 8 0 000 16zm7-8A7 7 0 111 8a7 7 0 0114 0z"/>
        </svg>
        {{ schedule.cron_expr }}
      </span>
      <span class="meta-item">次回: {{ formatNextRun(schedule.next_run_at) }}</span>
      <span v-if="schedule.is_channel" class="badge-channel">チャンネル監視</span>
    </div>

    <div v-if="schedule.last_error" class="card-error">
      <span class="error-icon">⚠</span> {{ schedule.last_error }}
      <span class="fail-count">({{ schedule.fail_count }}/3)</span>
    </div>

    <div class="card-actions">
      <button class="btn-action" @click="emit('runNow', schedule.id)" title="今すぐ実行">
        <svg viewBox="0 0 16 16" fill="currentColor" class="action-icon">
          <path d="M11.596 8.697l-6.363 3.692c-.54.313-1.233-.066-1.233-.697V4.308c0-.63.692-1.01 1.233-.696l6.363 3.692a.802.802 0 010 1.393z"/>
        </svg>
      </button>
      <button class="btn-action" @click="emit('edit', schedule)" title="編集">
        <svg viewBox="0 0 16 16" fill="currentColor" class="action-icon">
          <path d="M12.146.146a.5.5 0 01.708 0l3 3a.5.5 0 010 .708l-10 10a.5.5 0 01-.168.11l-5 2a.5.5 0 01-.65-.65l2-5a.5.5 0 01.11-.168l10-10zM11.207 2.5L13.5 4.793 14.793 3.5 12.5 1.207 11.207 2.5zm1.586 3L10.5 3.207 4 9.707V10h.5a.5.5 0 01.5.5v.5h.5a.5.5 0 01.5.5v.5h.293l6.5-6.5zm-9.761 5.175l-.106.106-1.528 3.821 3.821-1.528.106-.106A.5.5 0 015 12.5V12h-.5a.5.5 0 01-.5-.5V11h-.5a.5.5 0 01-.468-.325z"/>
        </svg>
      </button>
      <button class="btn-action btn-danger" @click="emit('delete', schedule.id)" title="削除">
        <svg viewBox="0 0 16 16" fill="currentColor" class="action-icon">
          <path d="M5.5 5.5A.5.5 0 016 6v6a.5.5 0 01-1 0V6a.5.5 0 01.5-.5zm2.5 0a.5.5 0 01.5.5v6a.5.5 0 01-1 0V6a.5.5 0 01.5-.5zm3 .5a.5.5 0 00-1 0v6a.5.5 0 001 0V6z"/>
          <path fill-rule="evenodd" d="M14.5 3a1 1 0 01-1 1H13v9a2 2 0 01-2 2H5a2 2 0 01-2-2V4h-.5a1 1 0 01-1-1V2a1 1 0 011-1H6a1 1 0 011-1h2a1 1 0 011 1h3.5a1 1 0 011 1v1zM4.118 4L4 4.059V13a1 1 0 001 1h6a1 1 0 001-1V4.059L11.882 4H4.118zM2.5 3V2h11v1h-11z" clip-rule="evenodd"/>
        </svg>
      </button>
    </div>
  </div>
</template>

<style scoped>
.schedule-card {
  background: var(--color-sidebar-bg);
  border: 1px solid var(--color-separator);
  border-radius: 0.75rem;
  padding: 1rem;
  transition: opacity 0.2s;
}
.schedule-card.inactive { opacity: 0.5; }
.card-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.5rem; }
.card-name { font-weight: 600; font-size: 0.9375rem; }
.card-meta { display: flex; gap: 0.75rem; font-size: 0.8125rem; color: rgba(120,120,128,0.8); flex-wrap: wrap; margin-bottom: 0.5rem; }
.meta-item { display: flex; align-items: center; gap: 0.25rem; }
.meta-icon { width: 0.875rem; height: 0.875rem; }
.badge-channel { background: var(--color-accent); color: white; font-size: 0.625rem; padding: 0.1rem 0.4rem; border-radius: 999px; }
.card-error { font-size: 0.75rem; color: #ff3b30; margin-bottom: 0.5rem; }
.fail-count { opacity: 0.7; }
.card-actions { display: flex; gap: 0.5rem; justify-content: flex-end; }
.btn-action { background: transparent; border: 1px solid var(--color-separator); border-radius: 0.375rem; padding: 0.25rem; cursor: pointer; color: inherit; }
.btn-danger { color: #ff3b30; }
.action-icon { width: 1rem; height: 1rem; }
.toggle-switch { position: relative; display: inline-block; width: 36px; height: 20px; }
.toggle-switch input { opacity: 0; width: 0; height: 0; }
.slider { position: absolute; inset: 0; background: #ccc; border-radius: 20px; cursor: pointer; transition: 0.2s; }
.slider::before { content: ''; position: absolute; height: 14px; width: 14px; left: 3px; bottom: 3px; background: white; border-radius: 50%; transition: 0.2s; }
input:checked + .slider { background: var(--color-accent); }
input:checked + .slider::before { transform: translateX(16px); }
</style>
```

- [ ] **Step 2: 型チェック**

```bash
npx vue-tsc --noEmit 2>&1 | tail -5
```

- [ ] **Step 3: コミット**

```bash
git add src/components/schedules/ScheduleCard.vue
git commit --no-gpg-sign -m "feat(schedules): add ScheduleCard component"
```

---

## Task 11: ScheduleDialog コンポーネント

**Files:**
- Create: `src/components/schedules/ScheduleDialog.vue`

- [ ] **Step 1: ScheduleDialog.vue を作成**

```vue
<script setup lang="ts">
import { ref, computed, watch } from 'vue'
import { Cron } from 'croner'
import type { Schedule, DownloadOptions } from '../../types'
import { useSettingsStore } from '../../stores/settings'

const props = defineProps<{
  schedule?: Schedule | null
  initialUrl?: string
  initialOptions?: DownloadOptions | null
}>()

const emit = defineEmits<{
  save: [payload: {
    name: string; url: string; cron_expr: string
    options_json: string; is_channel: boolean
  }]
  cancel: []
}>()

const settingsStore = useSettingsStore()

const name = ref(props.schedule?.name ?? '')
const url = ref(props.schedule?.url ?? props.initialUrl ?? '')
const cronExpr = ref(props.schedule?.cron_expr ?? '0 9 * * *')
const isChannel = ref(props.schedule?.is_channel ?? false)
const optionsJson = ref(props.schedule?.options_json ?? JSON.stringify({
  format: settingsStore.settings.default_video_format ?? 'mp4',
  quality: settingsStore.settings.default_video_quality ?? 'best',
  output_dir: settingsStore.settings.download_dir ?? '~/Downloads/YTDown/',
  embed_thumbnail: true,
  embed_metadata: true,
  write_subs: false,
  embed_subs: false,
  embed_chapters: true,
  sponsorblock: false,
  playlist_mode: 'single',
}))

const cronError = ref('')
const nextRuns = ref<string[]>([])

// cron式のバリデーションと次回5回プレビュー
watch(cronExpr, (expr) => {
  try {
    const job = new Cron(expr)
    cronError.value = ''
    const runs: string[] = []
    let next = job.nextRun()
    for (let i = 0; i < 5 && next; i++) {
      runs.push(next.toLocaleString('ja-JP'))
      next = job.nextRun(next)
    }
    nextRuns.value = runs
  } catch {
    cronError.value = '無効なcron式です'
    nextRuns.value = []
  }
}, { immediate: true })

const isValid = computed(() =>
  name.value.trim() !== '' &&
  url.value.trim() !== '' &&
  cronError.value === ''
)

function onSave() {
  if (!isValid.value) return
  emit('save', {
    name: name.value.trim(),
    url: url.value.trim(),
    cron_expr: cronExpr.value.trim(),
    options_json: optionsJson.value,
    is_channel: isChannel.value,
  })
}
</script>

<template>
  <div class="dialog-overlay" @click.self="emit('cancel')">
    <div class="dialog">
      <h2 class="dialog-title">{{ schedule ? 'スケジュールを編集' : 'スケジュールを追加' }}</h2>

      <div class="field">
        <label class="label">名前</label>
        <input v-model="name" class="input" placeholder="例: 深夜バックアップ" />
      </div>

      <div class="field">
        <label class="label">URL</label>
        <input v-model="url" class="input" placeholder="https://..." />
      </div>

      <div class="field">
        <label class="label">
          <input type="checkbox" v-model="isChannel" class="mr-2" />
          チャンネル監視（新着のみ）
        </label>
      </div>

      <div class="field">
        <label class="label">cron式</label>
        <input v-model="cronExpr" class="input font-mono" placeholder="0 9 * * *" />
        <p v-if="cronError" class="error-text">{{ cronError }}</p>
        <div v-if="nextRuns.length" class="next-runs">
          <p class="next-runs-label">次回5回の実行予定:</p>
          <ul>
            <li v-for="(run, i) in nextRuns" :key="i">{{ run }}</li>
          </ul>
        </div>
      </div>

      <div class="dialog-actions">
        <button class="btn btn-cancel" @click="emit('cancel')">キャンセル</button>
        <button class="btn btn-save" :disabled="!isValid" @click="onSave">
          {{ schedule ? '保存' : 'スケジュール登録' }}
        </button>
      </div>
    </div>
  </div>
</template>

<style scoped>
.dialog-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.4); display: flex; align-items: center; justify-content: center; z-index: 50; }
.dialog { background: var(--color-sidebar-bg); border: 1px solid var(--color-separator); border-radius: 1rem; padding: 1.5rem; width: 480px; max-width: 90vw; backdrop-filter: blur(20px); }
.dialog-title { font-size: 1.125rem; font-weight: 700; margin-bottom: 1.25rem; }
.field { margin-bottom: 1rem; }
.label { display: block; font-size: 0.8125rem; font-weight: 600; margin-bottom: 0.375rem; color: rgba(100,100,108,0.9); }
.input { width: 100%; padding: 0.5rem 0.75rem; border: 1px solid var(--color-separator); border-radius: 0.5rem; background: rgba(0,0,0,0.03); font-size: 0.875rem; box-sizing: border-box; }
.font-mono { font-family: 'SF Mono', 'Menlo', monospace; }
.error-text { color: #ff3b30; font-size: 0.75rem; margin-top: 0.25rem; }
.next-runs { margin-top: 0.5rem; font-size: 0.75rem; color: rgba(100,100,108,0.8); }
.next-runs-label { font-weight: 600; margin-bottom: 0.25rem; }
.next-runs ul { list-style: disc; margin-left: 1.25rem; space-y: 0.125rem; }
.dialog-actions { display: flex; justify-content: flex-end; gap: 0.75rem; margin-top: 1.5rem; }
.btn { padding: 0.5rem 1.25rem; border-radius: 0.5rem; font-size: 0.875rem; font-weight: 600; cursor: pointer; border: none; }
.btn-cancel { background: rgba(120,120,128,0.12); color: inherit; }
.btn-save { background: var(--color-accent); color: white; }
.btn-save:disabled { opacity: 0.4; cursor: not-allowed; }
</style>
```

- [ ] **Step 2: 型チェック**

```bash
npx vue-tsc --noEmit 2>&1 | tail -5
```

- [ ] **Step 3: コミット**

```bash
git add src/components/schedules/ScheduleDialog.vue
git commit --no-gpg-sign -m "feat(schedules): add ScheduleDialog component with cron validation"
```

---

## Task 12: ScheduleView コンポーネント

**Files:**
- Create: `src/components/schedules/ScheduleView.vue`

- [ ] **Step 1: ScheduleView.vue を作成**

```vue
<script setup lang="ts">
import { onMounted, ref } from 'vue'
import { useSchedulesStore } from '../../stores/schedules'
import type { Schedule } from '../../types'
import ScheduleCard from './ScheduleCard.vue'
import ScheduleDialog from './ScheduleDialog.vue'

const store = useSchedulesStore()
const showDialog = ref(false)
const editTarget = ref<Schedule | null>(null)

onMounted(async () => {
  await store.fetchSchedules()
  await store.setupScheduleListener()
})

function openCreate() {
  editTarget.value = null
  showDialog.value = true
}

function openEdit(schedule: Schedule) {
  editTarget.value = schedule
  showDialog.value = true
}

async function onSave(payload: {
  name: string; url: string; cron_expr: string
  options_json: string; is_channel: boolean
}) {
  if (editTarget.value) {
    await store.updateSchedule({ id: editTarget.value.id, ...payload })
  } else {
    await store.createSchedule(payload)
  }
  showDialog.value = false
}

async function onDelete(id: number) {
  if (confirm('このスケジュールを削除しますか？')) {
    await store.deleteSchedule(id)
  }
}
</script>

<template>
  <div class="schedule-view">
    <div class="view-header">
      <h2 class="view-title">スケジュール</h2>
      <button class="btn-add" @click="openCreate">
        <svg viewBox="0 0 20 20" fill="currentColor" class="add-icon">
          <path fill-rule="evenodd" d="M10 3a1 1 0 011 1v5h5a1 1 0 110 2h-5v5a1 1 0 11-2 0v-5H4a1 1 0 110-2h5V4a1 1 0 011-1z" clip-rule="evenodd"/>
        </svg>
        新規スケジュール
      </button>
    </div>

    <div v-if="store.schedules.length === 0" class="empty-state">
      <p>スケジュールはありません</p>
      <button class="btn-add-empty" @click="openCreate">最初のスケジュールを追加</button>
    </div>

    <div v-else class="cards-grid">
      <ScheduleCard
        v-for="s in store.schedules"
        :key="s.id"
        :schedule="s"
        @toggle="(id, isActive) => store.toggleSchedule(id, isActive)"
        @edit="openEdit"
        @delete="onDelete"
        @run-now="(id) => store.runNow(id)"
      />
    </div>

    <ScheduleDialog
      v-if="showDialog"
      :schedule="editTarget"
      @save="onSave"
      @cancel="showDialog = false"
    />
  </div>
</template>

<style scoped>
.schedule-view { padding: 1.5rem; height: 100%; overflow-y: auto; }
.view-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1.5rem; }
.view-title { font-size: 1.25rem; font-weight: 700; }
.btn-add { display: flex; align-items: center; gap: 0.375rem; background: var(--color-accent); color: white; border: none; padding: 0.5rem 1rem; border-radius: 0.5rem; font-size: 0.875rem; font-weight: 600; cursor: pointer; }
.add-icon { width: 1rem; height: 1rem; }
.empty-state { text-align: center; padding: 3rem; color: rgba(120,120,128,0.7); }
.btn-add-empty { margin-top: 1rem; background: rgba(0,122,255,0.1); color: var(--color-accent); border: 1px solid var(--color-accent); padding: 0.5rem 1rem; border-radius: 0.5rem; cursor: pointer; }
.cards-grid { display: grid; gap: 0.75rem; }
</style>
```

- [ ] **Step 2: 型チェック**

```bash
npx vue-tsc --noEmit 2>&1 | tail -5
```

- [ ] **Step 3: コミット**

```bash
git add src/components/schedules/ScheduleView.vue
git commit --no-gpg-sign -m "feat(schedules): add ScheduleView component"
```

---

## Task 13: サイドバー・App.vue 統合

**Files:**
- Modify: `src/components/layout/AppSidebar.vue`
- Modify: `src/App.vue`

- [ ] **Step 1: AppSidebar.vue にスケジュールセクションを追加**

`section-divider` (画像セクションの後) と 設定の間に追加:

```html
<!-- スケジュール section -->
<div class="sidebar-section">
  <h3 class="section-header">スケジュール</h3>
  <ul class="section-list">
    <li>
      <button class="sidebar-item" :class="{ active: isActive('schedules') }"
              @click="emit('update:currentSection', 'schedules')">
        <span class="item-indicator" />
        <svg class="item-icon" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
        </svg>
        <span class="item-label">スケジュール管理</span>
      </button>
    </li>
  </ul>
</div>

<div class="section-divider" />
```

- [ ] **Step 2: App.vue を更新**

`sectionLabel` の `labels` オブジェクトに追加:
```typescript
'schedules': 'スケジュール管理',
```

スケジュール画面のインポートと表示ロジックを追加:

```typescript
import ScheduleView from './components/schedules/ScheduleView.vue'
```

テンプレートのセクション切り替えに追加:
```html
<ScheduleView v-else-if="currentSection === 'schedules'" />
```

- [ ] **Step 3: 型チェック**

```bash
npx vue-tsc --noEmit 2>&1 | tail -5
```

- [ ] **Step 4: コミット**

```bash
git add src/components/layout/AppSidebar.vue src/App.vue
git commit --no-gpg-sign -m "feat(schedules): integrate schedule view into sidebar and App.vue"
```

---

## Task 14: DownloadDialog 統合

**Files:**
- Modify: `src/components/download/DownloadDialog.vue`

- [ ] **Step 1: DownloadDialog.vue にスケジュール登録トグルを追加**

既存の `DownloadDialog.vue` を読んで、以下を追加する:

1. `useSchedulesStore` をインポート
2. スクリプトに追加:
```typescript
import { useSchedulesStore } from '../../stores/schedules'
import ScheduleDialog from '../schedules/ScheduleDialog.vue'

const schedulesStore = useSchedulesStore()
const showScheduleMode = ref(false)
const showScheduleDialog = ref(false)
```

3. フォームの送信ボタンの上に追加:
```html
<!-- スケジュール実行トグル -->
<div class="schedule-toggle-row">
  <label class="toggle-label">
    <input type="checkbox" v-model="showScheduleMode" />
    <span class="toggle-text">スケジュール実行</span>
  </label>
</div>
```

4. 「ダウンロード」ボタンを条件分岐:
```html
<button v-if="!showScheduleMode" @click="onDownload">ダウンロード</button>
<button v-else @click="showScheduleDialog = true">スケジュール登録</button>

<ScheduleDialog
  v-if="showScheduleDialog"
  :initial-url="url"
  :initial-options="currentOptions"
  @save="onScheduleSave"
  @cancel="showScheduleDialog = false"
/>
```

5. ハンドラを追加:
```typescript
async function onScheduleSave(payload: { name: string; url: string; cron_expr: string; options_json: string; is_channel: boolean }) {
  await schedulesStore.createSchedule(payload)
  showScheduleDialog.value = false
  showScheduleMode.value = false
  // ダイアログを閉じる
  emit('close')
}
```

> `DownloadDialog.vue` の既存実装を先に読んでから、既存のコードパターンに合わせて実装すること。

- [ ] **Step 2: 型チェック**

```bash
npx vue-tsc --noEmit 2>&1 | tail -5
```

- [ ] **Step 3: 動作確認**

```bash
pnpm tauri dev
```

- [ ] **Step 4: コミット**

```bash
git add src/components/download/DownloadDialog.vue
git commit --no-gpg-sign -m "feat(schedules): add schedule toggle to DownloadDialog"
```

---

## Task 15: 最終ビルド確認とプッシュ

- [ ] **Step 1: Rust 最終チェック**

```bash
cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5
```

- [ ] **Step 2: TypeScript 最終チェック**

```bash
npx vue-tsc --noEmit 2>&1 | tail -5
```

- [ ] **Step 3: develop → main マージ**

```bash
git checkout main
git merge develop --no-ff -m "feat: schedule download feature v0.5.0"
git checkout develop
```

- [ ] **Step 4: リモートプッシュ**

```bash
git push origin develop && git push origin main
```
