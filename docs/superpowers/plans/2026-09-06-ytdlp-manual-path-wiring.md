# yt-dlp 手動パス設定の配線修正 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 設定画面の「yt-dlp パス」欄に入れた値が、実際の yt-dlp 検出（情報表示・更新確認・フォーマット取得・ダウンロード・スケジュール実行）で最優先に使われるようにする。

**Architecture:** バックエンドを唯一の真実の源にする。各コマンドは cookie 設定と同じ流儀で SQLite の `settings` テーブル（キー `ytdlp_path`）を読み、`detect_binary` に渡す。未配線だったメモリ上の `state.ytdlp_path` と Tauri コマンド `set_ytdlp_path` は撤去する。フロントはパス変更後に情報パネルを再検出し、失敗理由を表示する。

**Tech Stack:** Rust (Tauri v2, rusqlite, tokio, which, dirs), Vue 3 Composition API + TypeScript + Pinia + vue-i18n, vitest + @vue/test-utils (導入済み・テストファイルはまだ0件)

---

## Context

- 症状: homebrew 版 yt-dlp を削除すると「見つからない」エラー。設定欄に `~/.anyenv/envs/pyenv/shims/yt-dlp` を入れても無視され、再インストール後は常に `/usr/local/bin/yt-dlp` が採用される。
- 根本原因: `AdvancedSettings.vue:83` は `updateSetting('ytdlp_path', …)` → `set_setting` で DB に保存するだけ。一方 `binary::detect_binary(manual_path)` は `state.ytdlp_path`（`state.rs:30` で `None` 初期化）を読み、これを更新する `set_ytdlp_path` コマンド（`commands/settings.rs:7`）はフロントのどこからも呼ばれていない。DB の `ytdlp_path` を読む Rust コードも無い。初期コミット `b86c6b9` からこの状態。
- 副次的問題: `binary.rs` の `get_version` が終了コードを見ておらず、実行に失敗したバイナリでも空文字バージョンで「成功」扱いになる。
- 意図した仕様: `detect_binary` の docstring「manual path > system PATH > well-known paths > bundled」。UI ヒント「『auto』で自動検出、またはフルパスを入力」。
- 環境メモ（ユーザー実機）: Intel Mac。GUI 起動時の PATH は `/usr/bin:/bin:/usr/sbin:/sbin` のみ。pyenv 版 yt-dlp の実体は `/Users/annrie/.anyenv/envs/pyenv/versions/3.12.1/bin/yt-dlp`（2026.06.09）。deno / ffmpeg は `installed_on_request` なので homebrew の yt-dlp を消しても残る。

## 設計上の決定と注意（コードで検証済み）

- **手動パスは fail-fast**: 存在しない・実行できないパスが設定されていると、全コマンド（スケジュール実行含む）が「Manual yt-dlp path not found: …」等で失敗する。従来は黙って無視していたが、これが docstring どおりの仕様。UI はそのエラー文をそのまま表示する。
- **`resolve_binary` は `state.db` をスコープ内でロックして即解放する**。呼び出し側が `state.db` ロックを保持したまま呼ぶとデッドロックするため禁止。6箇所の呼び出し元はいずれもロック非保持（`resume_download` は直前で `drop(db)` 済み、`formats.rs` は検出後に DB を読む）ことを確認済み。
- **他に yt-dlp を解決する箇所は無い**: `scheduler.rs` と `images/` は `download.rs` の関数を経由する。`Command::new` の直接呼び出しは ffmpeg / osascript 等のみ。
- **`set_ytdlp_path` の参照は5箇所のみ**（`build.rs`、`capabilities/default.json`、`permissions/autogenerated/set_ytdlp_path.toml`、`commands/settings.rs`、`lib.rs`）。`gen/schemas` は gitignore 済みの生成物。`permissions/autogenerated` は 73 ファイルが git 管理下にあり `build.rs` の一覧より多い＝古い生成物は自動では消えないので、toml は手で削除する。
- **`AppState` の構築は `lib.rs:41` の1箇所だけ**。フィールド削除でテストや他モジュールは壊れない。
- **CI（`.github/workflows/validate.yml`）は `pnpm build` のみ**。`cargo test` と `vitest` はローカルが唯一の関門。新規テストファイルは `tsconfig` の `include` に入るため `vue-tsc --noEmit` で型検査される。
- 新しい依存は追加しない（`dirs` は既存依存、`@vue/test-utils` / `happy-dom` / `vitest` は導入済み）。

## File Map

| Action | File | 役割 |
|--------|------|------|
| Modify | `src-tauri/src/ytdlp/binary.rs` | 設定値の正規化 `manual_path_from_setting`、`get_version` の失敗検知、PATH 段の寛容化、単体テスト |
| Modify | `src-tauri/src/commands/ytdlp_mgmt.rs` | DB を読んで検出する `resolve_binary` ヘルパー、4コマンドの切り替え |
| Modify | `src-tauri/src/commands/download.rs` | 4箇所の呼び出しを `resolve_binary` に差し替え |
| Modify | `src-tauri/src/commands/formats.rs` | 2箇所の呼び出しを `resolve_binary` に差し替え |
| Modify | `src-tauri/src/state.rs` | `ytdlp_path` フィールド撤去 |
| Modify | `src-tauri/src/commands/settings.rs` | `set_ytdlp_path` 撤去 |
| Modify | `src-tauri/src/lib.rs` | `set_ytdlp_path` のハンドラ登録を撤去 |
| Modify | `src-tauri/build.rs` | `app_manifest` の一覧から `set_ytdlp_path` を撤去 |
| Modify | `src-tauri/capabilities/default.json` | `allow-set-ytdlp-path` を撤去 |
| Delete | `src-tauri/permissions/autogenerated/set_ytdlp_path.toml` | 生成物の掃除 |
| Modify | `src/components/settings/AdvancedSettings.vue` | 変更後に再検出、エラー表示、`data-testid` |
| Create | `src/components/settings/AdvancedSettings.test.ts` | リポジトリ初の vitest（保存→再検出の順序、エラー表示） |
| Modify | `README.md` | 手動パス設定の説明（英・日） |

再利用するもの: `crate::db::queries::get_setting` (`db/queries.rs:290`)、DB 読み出しパターン `get_cookie_settings` (`commands/download.rs:43`)、`useYtdlp().loadInfo` (`src/composables/useYtdlp.ts`)、`src/i18n/index.ts` の `i18n` インスタンス。

ブランチ: `fix/ytdlp-manual-path-wiring`（`main` から作成）。コミットは既存流儀（conventional + gitmoji + 日本語要約）。

---

## Task 1: binary.rs — 設定値の正規化と get_version の失敗検知（TDD）

**Files:**
- Modify: `src-tauri/src/ytdlp/binary.rs`

- [ ] **Step 1: 失敗するテストを追加**（ファイル末尾に追記）

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn manual_path_none_for_auto_empty_and_whitespace() {
        assert_eq!(manual_path_from_setting(None), None);
        assert_eq!(manual_path_from_setting(Some("auto")), None);
        assert_eq!(manual_path_from_setting(Some("")), None);
        assert_eq!(manual_path_from_setting(Some("   ")), None);
    }

    #[test]
    fn manual_path_trims_and_keeps_absolute_path() {
        assert_eq!(
            manual_path_from_setting(Some("  /usr/local/bin/yt-dlp \n")),
            Some("/usr/local/bin/yt-dlp".to_string())
        );
    }

    #[test]
    fn manual_path_expands_home_prefix() {
        let home = dirs::home_dir().expect("home dir");
        let expected = home.join(".local/bin/yt-dlp").to_string_lossy().to_string();
        assert_eq!(manual_path_from_setting(Some("~/.local/bin/yt-dlp")), Some(expected));
    }

    #[cfg(unix)]
    fn write_script(name: &str, body: &str) -> PathBuf {
        use std::os::unix::fs::PermissionsExt;
        let dir = std::env::temp_dir().join(format!("ytdown-binary-test-{}", std::process::id()));
        std::fs::create_dir_all(&dir).unwrap();
        let path = dir.join(name);
        std::fs::write(&path, body).unwrap();
        std::fs::set_permissions(&path, std::fs::Permissions::from_mode(0o755)).unwrap();
        path
    }

    #[cfg(unix)]
    #[test]
    fn get_version_returns_trimmed_stdout_on_success() {
        let script = write_script("ok.sh", "#!/bin/sh\necho 2026.01.01\n");
        assert_eq!(get_version(&script).unwrap(), "2026.01.01");
    }

    #[cfg(unix)]
    #[test]
    fn get_version_errors_on_nonzero_exit_and_includes_stderr() {
        let script = write_script("fail.sh", "#!/bin/sh\necho 'command not found' >&2\nexit 127\n");
        let err = get_version(&script).unwrap_err();
        assert!(err.contains("exited"), "unexpected error: {err}");
        assert!(err.contains("command not found"), "stderr missing: {err}");
    }

    #[cfg(unix)]
    #[test]
    fn get_version_errors_on_empty_output() {
        let script = write_script("empty.sh", "#!/bin/sh\nexit 0\n");
        let err = get_version(&script).unwrap_err();
        assert!(err.contains("empty version"), "unexpected error: {err}");
    }

    #[cfg(unix)]
    #[test]
    fn detect_binary_prefers_manual_path() {
        let script = write_script("manual.sh", "#!/bin/sh\necho manual-1.0\n");
        let bin = detect_binary(Some(script.to_str().unwrap())).unwrap();
        assert_eq!(bin.path, script);
        assert_eq!(bin.version, "manual-1.0");
        assert!(matches!(bin.managed_by, ManagedBy::Manual));
    }

    #[test]
    fn detect_binary_errors_when_manual_path_missing() {
        let err = detect_binary(Some("/nonexistent/ytdown/yt-dlp")).unwrap_err();
        assert!(err.contains("Manual yt-dlp path not found"), "unexpected error: {err}");
    }
}
```

- [ ] **Step 2: 失敗を確認**

Run: `cargo test --manifest-path src-tauri/Cargo.toml ytdlp::binary`
Expected: コンパイルエラー `cannot find function manual_path_from_setting`

- [ ] **Step 3: 実装**

`classify_managed_by` の直後（`detect_binary` の前）に追加:

```rust
/// Normalize the raw `ytdlp_path` setting value.
/// `None`, "auto", empty, or whitespace-only mean auto-detect. A leading "~/" is expanded.
pub fn manual_path_from_setting(raw: Option<&str>) -> Option<String> {
    let trimmed = raw?.trim();
    if trimmed.is_empty() || trimmed == "auto" {
        return None;
    }
    if let Some(rest) = trimmed.strip_prefix("~/") {
        if let Some(home) = dirs::home_dir() {
            return Some(home.join(rest).to_string_lossy().to_string());
        }
    }
    Some(trimmed.to_string())
}
```

`get_version` を置き換え:

```rust
fn get_version(path: &PathBuf) -> Result<String, String> {
    let output = Command::new(path)
        .arg("--version")
        .output()
        .map_err(|e| format!("Failed to run yt-dlp at {}: {}", path.display(), e))?;
    if !output.status.success() {
        let stderr = String::from_utf8_lossy(&output.stderr).trim().to_string();
        return Err(format!(
            "yt-dlp at {} exited with {}: {}",
            path.display(),
            output.status,
            stderr
        ));
    }
    let version = String::from_utf8_lossy(&output.stdout).trim().to_string();
    if version.is_empty() {
        return Err(format!("yt-dlp at {} returned an empty version", path.display()));
    }
    Ok(version)
}
```

`detect_binary` の「2. System PATH」段を、失敗時に次の段へ落ちるように変更（`get_version` が厳格になったため。手動パスと同梱は従来どおり `?` で即エラー）:

```rust
    // 2. System PATH (using `which` crate for cross-platform support).
    // A broken binary on PATH falls through to well-known paths / bundled.
    if let Ok(path) = which::which("yt-dlp") {
        if let Ok(version) = get_version(&path) {
            let path_str = path.to_string_lossy().to_string();
            let managed_by = classify_managed_by(&path_str);
            return Ok(YtdlpBinary {
                path,
                version,
                managed_by,
            });
        }
    }
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test --manifest-path src-tauri/Cargo.toml ytdlp::binary`
Expected: `test result: ok. 8 passed`

- [ ] **Step 5: コミット**

```bash
git add src-tauri/src/ytdlp/binary.rs
git commit -m "fix(ytdlp): 🐛 手動パス設定の正規化とバージョン取得の失敗検知を追加"
```

---

## Task 2: ytdlp_mgmt.rs — `resolve_binary` ヘルパーと4コマンドの切り替え

**Files:**
- Modify: `src-tauri/src/commands/ytdlp_mgmt.rs`

- [ ] **Step 1: import とヘルパーを追加**（ファイル先頭）

```rust
use crate::db::queries;
use crate::state::AppState;
use crate::ytdlp::binary::{self, YtdlpBinary};
use serde::Serialize;
use tauri::State;

/// Settings-table key holding the user's manual yt-dlp path ("auto" = auto-detect).
pub const YTDLP_PATH_SETTING_KEY: &str = "ytdlp_path";

/// Resolve the yt-dlp binary for a command: read the manual path from the settings DB
/// under a scoped lock, then run detection (manual > PATH > well-known > bundled)
/// off the async runtime because it spawns `yt-dlp --version`.
pub async fn resolve_binary(state: &AppState) -> Result<YtdlpBinary, String> {
    let raw = {
        let db = state.db.lock().await;
        queries::get_setting(&db, YTDLP_PATH_SETTING_KEY).ok().flatten()
    }; // db lock dropped here
    let manual = binary::manual_path_from_setting(raw.as_deref());
    tokio::task::spawn_blocking(move || binary::detect_binary(manual.as_deref()))
        .await
        .map_err(|e| format!("Task error: {}", e))?
}
```

- [ ] **Step 2: 4コマンドを切り替え**

`get_ytdlp_info`:
```rust
pub async fn get_ytdlp_info(state: State<'_, AppState>) -> Result<YtdlpInfo, String> {
    let bin = resolve_binary(&state).await?;
    // 以下 managed_by の match と Ok(YtdlpInfo{..}) は従来どおり
```

`check_ytdlp_update`:
```rust
pub async fn check_ytdlp_update(state: State<'_, AppState>) -> Result<Option<String>, String> {
    let current = resolve_binary(&state).await?.version;
    tokio::task::spawn_blocking(move || {
        let latest = binary::fetch_latest_github_version()?;
        if latest != current {
            Ok(Some(latest))
        } else {
            Ok(None)
        }
    })
    .await
    .map_err(|e| format!("Task failed: {}", e))?
}
```

`install_ytdlp` の再検出部分:
```rust
    // Re-detect to get full info
    let bin = resolve_binary(&state)
        .await
        .map_err(|_| format!("Installed but failed to detect at: {}", path.display()))?;
```

`update_ytdlp` の先頭:
```rust
pub async fn update_ytdlp(state: State<'_, AppState>) -> Result<String, String> {
    let bin = resolve_binary(&state).await?;
    match bin.managed_by {
```
（`let ytdlp_path = state.ytdlp_path.lock().await;` と `drop(ytdlp_path);` は削除。Bundled 分岐の `binary::detect_binary(None)` はそのまま）

- [ ] **Step 3: 型検査**

Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Expected: 成功（`state.ytdlp_path` はまだ存在するので他ファイルは未変更でも通る）

- [ ] **Step 4: コミット**

```bash
git add src-tauri/src/commands/ytdlp_mgmt.rs
git commit -m "refactor(ytdlp): ♻️ 設定DBから手動パスを読むresolve_binaryを追加しyt-dlp管理コマンドを切り替え"
```

---

## Task 3: download.rs / formats.rs の呼び出し箇所を差し替え

**Files:**
- Modify: `src-tauri/src/commands/download.rs`（`start_download` 約633行、`resume_download` 約796行、スケジューラ用関数 約914行、`fetch_playlist_items` 約1277行）
- Modify: `src-tauri/src/commands/formats.rs`（`fetch_formats` 約10行、`fetch_channel_info` 約186行）

- [ ] **Step 1: download.rs の4箇所**

`start_download`:
```rust
    let bin = crate::commands::ytdlp_mgmt::resolve_binary(&state).await?;
```
（`state.ytdlp_path.lock()` を含むブロックごと置換）

`resume_download` の「Process dead」分岐:
```rust
                let bin = crate::commands::ytdlp_mgmt::resolve_binary(&state).await?;
```

スケジューラ用関数（`let state = app.state::<AppState>();` の直後）:
```rust
    let bin = crate::commands::ytdlp_mgmt::resolve_binary(&state).await?;
    let ytdlp_path = bin.path.to_string_lossy().to_string();
```

`fetch_playlist_items`:
```rust
    let bin = crate::commands::ytdlp_mgmt::resolve_binary(&state).await?;
    let ytdlp_path = bin.path.to_string_lossy().to_string();
```
（`drop(ytdlp_path_lock);` を削除）

- [ ] **Step 2: formats.rs の2箇所**（どちらも同じ形）

```rust
    let binary = crate::commands::ytdlp_mgmt::resolve_binary(&state).await?;
```
（`state.ytdlp_path.lock()` / `path_clone` / `spawn_blocking(detect_binary)` の3〜6行を置換。先頭の `use crate::ytdlp::{binary, process};` は `binary` が未使用になれば `use crate::ytdlp::process;` に）

- [ ] **Step 3: 型検査と未使用 import の整理**

Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Expected: 成功。`unused import: binary` の警告が出たら download.rs / formats.rs の import を削る

- [ ] **Step 4: コミット**

```bash
git add src-tauri/src/commands/download.rs src-tauri/src/commands/formats.rs
git commit -m "fix(ytdlp): 🐛 ダウンロード/フォーマット取得/スケジュール実行で設定の手動パスを使うよう配線"
```

---

## Task 4: 未配線だった状態とコマンドの撤去

**Files:**
- Modify: `src-tauri/src/state.rs`、`src-tauri/src/commands/settings.rs`、`src-tauri/src/lib.rs`、`src-tauri/build.rs`、`src-tauri/capabilities/default.json`
- Delete: `src-tauri/permissions/autogenerated/set_ytdlp_path.toml`

- [ ] **Step 1: 撤去**
  - `state.rs`: 構造体の `pub ytdlp_path: Arc<Mutex<Option<String>>>,` と `new()` の `ytdlp_path: Arc::new(Mutex::new(None)),` を削除
  - `settings.rs`: `set_ytdlp_path` 関数（`#[tauri::command]` 含む）を削除
  - `lib.rs`: `commands::settings::set_ytdlp_path,` の行を削除
  - `build.rs`: `"set_ytdlp_path",` を削除
  - `capabilities/default.json`: `"allow-set-ytdlp-path",` を削除
  - `git rm src-tauri/permissions/autogenerated/set_ytdlp_path.toml`

- [ ] **Step 2: 残存参照が無いことを確認**

Run: `grep -rn "ytdlp_path\b\|set_ytdlp_path\|set-ytdlp-path" src-tauri/src src-tauri/build.rs src-tauri/capabilities src-tauri/permissions src`
Expected: ヒットは `YTDLP_PATH_SETTING_KEY` 周辺、`DownloadConfig.ytdlp_path` などの関数引数名、フロントの設定キー `ytdlp_path` のみ

- [ ] **Step 3: ビルドとテスト**

Run: `cargo check --manifest-path src-tauri/Cargo.toml && cargo test --manifest-path src-tauri/Cargo.toml`
Expected: 成功

- [ ] **Step 4: コミット**

```bash
git add -A src-tauri
git commit -m "refactor(ytdlp): 🔥 未配線だったset_ytdlp_pathとメモリ上のytdlp_pathを撤去"
```

---

## Task 5: フロント — パス変更後の再検出とエラー表示（TDD）

**Files:**
- Create: `src/components/settings/AdvancedSettings.test.ts`
- Modify: `src/components/settings/AdvancedSettings.vue`

- [ ] **Step 1: 失敗するテストを作成**

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { mount, flushPromises } from '@vue/test-utils'
import { createPinia } from 'pinia'
import { i18n } from '../../i18n'
import AdvancedSettings from './AdvancedSettings.vue'

const { invokeMock } = vi.hoisted(() => ({ invokeMock: vi.fn() }))
vi.mock('@tauri-apps/api/core', () => ({ invoke: invokeMock }))

function ytdlpInfo(path: string) {
  return { path, version: '2026.06.09', update_available: false, latest_version: null, managed_by: 'manual' }
}

function mountComponent() {
  return mount(AdvancedSettings, { global: { plugins: [i18n, createPinia()] } })
}

describe('AdvancedSettings — yt-dlp path', () => {
  beforeEach(() => {
    invokeMock.mockReset()
  })

  it('saves the path on change, then re-resolves yt-dlp info', async () => {
    const newPath = '/Users/me/.anyenv/envs/pyenv/versions/3.12.1/bin/yt-dlp'
    let currentPath = '/usr/local/bin/yt-dlp'
    invokeMock.mockImplementation(async (cmd: string, args?: Record<string, unknown>) => {
      if (cmd === 'set_setting' && args?.key === 'ytdlp_path') currentPath = String(args.value)
      if (cmd === 'get_ytdlp_info') return ytdlpInfo(currentPath)
      return undefined
    })

    const wrapper = mountComponent()
    await flushPromises()
    expect(wrapper.text()).toContain('/usr/local/bin/yt-dlp')

    const input = wrapper.get('input[data-testid="ytdlp-path"]')
    ;(input.element as HTMLInputElement).value = newPath
    await input.trigger('change')
    await flushPromises()

    expect(invokeMock).toHaveBeenCalledWith('set_setting', { key: 'ytdlp_path', value: newPath })
    const order = invokeMock.mock.calls.map((c) => c[0])
    expect(order.lastIndexOf('set_setting')).toBeLessThan(order.lastIndexOf('get_ytdlp_info'))
    expect(wrapper.text()).toContain(newPath)
  })

  it('shows the backend error when yt-dlp cannot be resolved', async () => {
    invokeMock.mockImplementation(async (cmd: string) => {
      if (cmd === 'get_ytdlp_info') throw 'Manual yt-dlp path not found: /nope/yt-dlp'
      return undefined
    })

    const wrapper = mountComponent()
    await flushPromises()

    expect(wrapper.text()).toContain('Manual yt-dlp path not found: /nope/yt-dlp')
  })
})
```

- [ ] **Step 2: 失敗を確認**

Run: `npx vitest run src/components/settings/AdvancedSettings.test.ts`
Expected: 2件とも FAIL（`data-testid` が無い / エラー文が描画されない）

- [ ] **Step 3: 実装**

`<script setup>` に追加:
```ts
async function onYtdlpPathChange(event: Event) {
  const value = (event.target as HTMLInputElement).value
  await settingsStore.updateSetting('ytdlp_path', value)
  await loadInfo()
}
```

パス入力欄を置き換え（毎キー保存の `@input` をやめ、確定時に保存→再検出。半端なパスが DB に残らない）:
```vue
      <input :value="settingsStore.settings.ytdlp_path"
             data-testid="ytdlp-path"
             @change="onYtdlpPathChange"
             class="w-full h-8 px-3 rounded-md bg-neutral-100 dark:bg-neutral-800 text-sm font-mono outline-none focus:ring-1 focus:ring-[var(--color-accent)]"
             :placeholder="t('general.ytdlp_path_placeholder')" />
```

未検出ブロックにエラー本文を出す:
```vue
      <div v-else class="text-xs text-red-400">
        {{ t('general.ytdlp_not_found') }}
        <span v-if="ytdlpError" class="block font-mono mt-1 break-all">{{ ytdlpError }}</span>
      </div>
```

- [ ] **Step 4: テストと型検査**

Run: `npx vitest run && npx vue-tsc --noEmit`
Expected: `2 passed`、型エラーなし

- [ ] **Step 5: コミット**

```bash
git add src/components/settings/AdvancedSettings.vue src/components/settings/AdvancedSettings.test.ts
git commit -m "fix(settings): 🐛 yt-dlpパス変更時に再検出しエラー内容を表示"
```

---

## Task 6: README（英・日）

**Files:**
- Modify: `README.md`（英: 122〜125行「yt-dlp」箇条書き、日: 274〜277行）

- [ ] **Step 1: 英語側の箇条書き末尾に追加**

```markdown
  - Using yt-dlp from pyenv / pipx / pip instead of Homebrew? Enter its full path in **Settings → Advanced → yt-dlp path** (`~/` is expanded). Leave `auto` for auto-detection.
```

- [ ] **Step 2: 日本語側の箇条書き末尾に追加**

```markdown
  - Homebrew 以外（pyenv / pipx / pip）の yt-dlp を使う場合は、**設定 → 詳細設定 → yt-dlp パス** にフルパスを入力してください（`~/` 展開可）。`auto` のままなら自動検出です。
```

- [ ] **Step 3: コミット**

```bash
git add README.md
git commit -m "docs: 📝 READMEにyt-dlpパス設定の説明を追記（英日）"
```

---

## Verification（実装完了後）

1. 自動テスト
   - `cargo check --manifest-path src-tauri/Cargo.toml`
   - `cargo test --manifest-path src-tauri/Cargo.toml`
   - `npx vue-tsc --noEmit`
   - `npx vitest run`
2. 手動 E2E（`pnpm tauri dev`）
   - 設定 → 詳細設定 → yt-dlp パスに `/Users/annrie/.anyenv/envs/pyenv/versions/3.12.1/bin/yt-dlp` を入れて確定 → 情報パネルがそのパス・バージョン `2026.06.09`・管理「手動」に変わる
   - 存在しないパスを入れる → 「Manual yt-dlp path not found: …」が表示される
   - `auto` に戻す → `/usr/local/bin/yt-dlp` に戻る
   - 手動パス設定のまま任意の動画 URL でフォーマット取得とダウンロードが成功する
   - アプリ再起動後も手動パスが維持される（DB 由来なので保持されるはず）
3. セルフレビュー → `@codex-rescue` をフォアグラウンドで実行 → 指摘対応
4. PR 作成（本文は英日併記）→ Codex 自動レビュー対応（指摘が尽きるまで）→ マージ → `develop` を `main` と同期
5. 計画の写しを `docs/superpowers/plans/2026-09-06-ytdlp-manual-path-wiring.md` に保存（重要決定の記録）

## マージ後のユーザー作業（アプリ側では行わない）

1. pyenv の yt-dlp を更新: `~/.anyenv/envs/pyenv/versions/3.12.1/bin/python -m pip install -U yt-dlp`
2. 修正版アプリで yt-dlp パスに上記の実体パスを設定し、情報パネルで反映を確認
3. その後で `brew uninstall yt-dlp`（deno / ffmpeg は on-request なので残る）
